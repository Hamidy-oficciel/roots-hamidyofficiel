using UnityEngine;

namespace ROOTS.Core
{
    /// <summary>
    /// Minimal startup entry point for the ROOTS application.
    /// This script intentionally remains lightweight and will be expanded
    /// as initialization requirements are defined.
    /// </summary>
    public class Bootstrap : MonoBehaviour
    {
        /// <summary>
        /// Called when the script instance is being loaded.
        /// Reserved for early initialization work.
        /// </summary>
        private void Awake()
        {
            // TODO: Add early initialization logic when required.
        }

        /// <summary>
        /// Called before the first frame update.
        /// Reserved for startup sequencing and future setup tasks.
        /// </summary>
        private void Start()
        {
            // TODO: Add startup sequencing logic when required.
        }
    }
}
